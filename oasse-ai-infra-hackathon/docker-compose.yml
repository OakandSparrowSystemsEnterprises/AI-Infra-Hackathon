services:
  authority-demo:
    build: .
    ports:
      - "8000:8000"
    environment:
      AUTHORITY_MODE: reference
      EVIDENCE_MAX_AGE_MS: "500"
      MIN_CONFIDENCE: "0.80"
      MAX_SPEED_MPS: "0.35"
