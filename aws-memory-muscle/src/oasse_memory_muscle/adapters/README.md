# Adapter Directory

Implement real sponsor integrations here after build open.

Suggested modules are `cognee.py`, `hydradb.py`, `hotdata.py`, `tenki.py`, `snyk.py`, `rote.py`, `gatekeeper.py`, and `github_effect.py`.

Keep Gatekeeper external. Keep unrestricted GitHub write credentials out of Tenki. Every adapter should return explicit evidence references suitable for the Action Envelope and Learning Event.
